specVersion: "v1"
serviceId: "campusbite"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "campusbite-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
